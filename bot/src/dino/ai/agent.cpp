#include "agent.hpp"

namespace dino::ai
{
	void agent::run()
	{
		switch (state_)
		{
		case (state::idle):
			// find next goal
			break;

		case (state::move_to):
			//
			break;

		case (state::perform_action):
			break;
		}
	}
}