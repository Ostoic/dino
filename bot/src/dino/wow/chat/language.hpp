#pragma once

#include "../guid.hpp"

// Idea: Make this an enum of all possible languages, then have a player-owned object
//  - representing the languages that the player actually knows
namespace dino::wow::chat
{
	enum class language
	{
		common = 0,

	};
}
